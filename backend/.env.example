# ========================
# CADERNETA API - .env.example
# ========================
# Copie este arquivo para .env.local, .env.staging ou .env.production
# e preencha os valores de acordo com seu ambiente

# ========================
# AMBIENTE
# ========================
NODE_ENV=development

# ========================
# SERVIDOR
# ========================
# Porta em que o servidor Express irá rodar
API_PORT=3000

# URL base da API (usada para referências em emails, docs, etc)
API_URL=http://localhost:3000

# ========================
# BANCO DE DADOS (PostgreSQL)
# ========================
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=seu_senha_postgres
DB_NAME=caderneta

# ========================
# AUTENTICAÇÃO (JWT)
# ========================
# Secret para assinar access tokens
# Em produção, use uma string aleatória e segura (mínimo 32 caracteres)
JWT_SECRET=sua_chave_secreta_muito_segura_aqui_em_producao

# Tempo de expiração do access token
JWT_EXPIRE=24h

# Secret para assinar refresh tokens
# Em produção, use uma string aleatória e segura (diferente do JWT_SECRET)
JWT_REFRESH_SECRET=sua_chave_secreta_refresh_muito_segura_aqui_em_producao

# Tempo de expiração do refresh token
JWT_REFRESH_EXPIRE=7d

# ========================
# CORS (Cross-Origin Resource Sharing)
# ========================
# URLs permitidas para requisições CORS (separadas por vírgula)
# Exemplo: http://localhost:3001,http://localhost:5173,https://seu-frontend.com
CORS_ORIGIN=http://localhost:3001,http://localhost:5173

# ========================
# RATE LIMITING
# ========================
# Janela de tempo em minutos para contar requisições
RATE_LIMIT_WINDOW=15

# Número máximo de requisições por janela
RATE_LIMIT_MAX_REQUESTS=100
