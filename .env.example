# ==========================================
# CADERNETA - Exemplo de Arquivo .env
# ==========================================
# Este arquivo serve como referência para as variáveis necessárias
# Copie para .env.local, .env.staging ou .env.production e ajuste os valores

# ==========================================
# AMBIENTE
# ==========================================
# Valores: development, staging, production
NODE_ENV=development

# ==========================================
# SERVIDOR
# ==========================================
# Porta em que a API será executada
API_PORT=8080

# URL pública da API (usada pelo frontend)
API_URL=http://localhost:8080

# ==========================================
# BANCO DE DADOS (PostgreSQL)
# ==========================================
# Host do banco de dados
# - Desenvolvimento local: localhost
# - Neon (staging/prod): seu-host-neon.neon.tech
DB_HOST=localhost

# Porta do PostgreSQL (padrão: 5432)
DB_PORT=5432

# Usuário do banco de dados (padrão local: postgres)
DB_USER=postgres

# Senha do banco de dados
DB_PASSWORD=sua_senha_aqui

# Nome do banco de dados
DB_NAME=caderneta

# ==========================================
# AUTENTICAÇÃO JWT
# ==========================================
# Chave secreta para assinar tokens JWT
# Gerar com: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-in-production-12345

# Chave secreta para refresh tokens
JWT_REFRESH_SECRET=your-super-secret-refresh-key-change-in-production-12345

# Tempo de expiração do access token
JWT_EXPIRE=24h

# Tempo de expiração do refresh token
JWT_REFRESH_EXPIRE=7d

# ==========================================
# RATE LIMITING
# ==========================================
# Janela de tempo para rate limiting (em minutos)
RATE_LIMIT_WINDOW=15

# Número máximo de requisições por janela de tempo
RATE_LIMIT_MAX_REQUESTS=100

# ==========================================
# FRONTEND - Variáveis Expo
# ==========================================
# URL da API para o frontend Expo
EXPO_PUBLIC_API_URL=http://192.168.0.138:8080
